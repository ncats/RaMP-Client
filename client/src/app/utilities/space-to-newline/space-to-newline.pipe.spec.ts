import { SpaceToNewlinePipe } from './space-to-newline.pipe';

describe('SpaceToNewlinePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToNewlinePipe();
    expect(pipe).toBeTruthy();
  });
});
